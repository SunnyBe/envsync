apiVersion: v2
name: envsync
description: A Helm chart for deploying EnvSync to a Kubernetes cluster.
type: application
version: '0.1.0'
appVersion: '1.0.0'

dependencies:
  - name: postgresql
    version: '18.5.11'
    repository: https://charts.bitnami.com/bitnami
